function multiplica(x: number, y: number): number {
    return x * y;
}

const resultado = multiplica(10, 3);
console.log(resultado);

function dizOi(nome: string): string {
    return "Olá " + nome;
}

const mesnsagem = dizOi("Lucas");
console.log(mesnsagem);